language: cpp

compiler:
  - clang
  - gcc

before_script:
  - sh ./scripts/ArangoDB/arangodb_startup.sh
  - sudo apt-get update -qq
  - sudo apt-get install -qq qtbase5-dev
  - sh ./scripts/travis-ci/build-driver.sh

script:
  - sh ./scripts/travis-ci/build-tests.sh
